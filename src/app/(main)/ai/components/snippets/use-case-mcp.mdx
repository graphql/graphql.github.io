```graphql filename="Example query"
# AI agent discovers and calls tools
query {
  tools {
    name
    description
    parameters {
      name
      type
    }
  }
}
mutation {
  callTool(
    name: "searchProducts"
    params: { query: "laptop" }
  )
}
```
